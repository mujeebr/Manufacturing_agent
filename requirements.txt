# ============================================================
# requirements.txt
# Purpose: Pin the Python dependencies needed to run the
# Manufacturing Multi-Agent System (orchestrator + deep agent +
# subagent + response agent) built on LangGraph + OpenAI.
#
# Install with:
#   python -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
# ============================================================

# --- Core LangGraph / LangChain stack ---
langgraph==0.2.60                 # graph orchestration engine (nodes, edges, state)
langgraph-checkpoint-sqlite==2.0.1  # SQLite-backed checkpointer for persistence/HITL
langchain==0.3.14                 # base abstractions (messages, tools, prompts)
langchain-openai==0.2.14          # ChatOpenAI wrapper for GPT models
langchain-core==0.3.29            # core message/runnable primitives

# --- OpenAI SDK (used directly in a couple of utility spots) ---
openai==1.59.6

# --- Data layer (SQLite manufacturing database) ---
SQLAlchemy==2.0.36                # optional ORM-style helper for schema/seed scripts
faker==33.1.0                     # generates realistic synthetic manufacturing data

# --- Visualization (used by the visualisation sub agent) ---
matplotlib==3.11.1              # 3.10.0 RecursionError on Python 3.14 Path.__deepcopy__
pandas==2.2.3

# --- Config / environment ---
python-dotenv==1.0.1               # loads OPENAI_API_KEY etc. from a .env file

# --- CLI / display niceties ---
rich==13.9.4                       # pretty console output for human-in-the-loop prompts
streamlit==1.41.1                  # step-through UI (streamlit_app.py)